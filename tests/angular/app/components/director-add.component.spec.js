ngDescribe({
    name: 'Test director-add component',
    modules: 'app',
    element: '<director-add></director-add>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
