ngDescribe({
    name: 'Test pending-complete component',
    modules: 'app',
    element: '<pending-complete></pending-complete>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
