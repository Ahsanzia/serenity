ngDescribe({
    name: 'Test cost-add component',
    modules: 'app',
    element: '<cost-add></cost-add>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
