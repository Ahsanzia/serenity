ngDescribe({
    name: 'Test client-add component',
    modules: 'app',
    element: '<client-add></client-add>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
